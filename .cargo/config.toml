[alias]
dev = "run --package dev-runner --bin dev"

[build]
target-dir = "target"

[env]
# Optimize for smaller builds in development
CARGO_PROFILE_DEV_DEBUG = "1"
CARGO_PROFILE_DEV_INCREMENTAL = "true"
